# Status

## Done
- **Footer made site-wide + spacing tuned (2026-07-22, this session):** The ownership footer (commit 24f5d6d) was nested inside `MenuTab`, so it only rendered on the Menu tab. Moved it out into the App shell's content column (right after the tab-render block in `src/App.jsx`) so it now shows on ALL tabs (Dashboard/Sales/Scan/Menu/Suppliers/Insights/Security) — verified across every tab on the demo, exactly one footer instance. Added a `1px T.border` top divider + `marginTop 36/24`. Spacing = "Option B" (tighter below than above): the content column has its own 28px/16px bottom padding sitting under the footer, so footer `paddingBottom` zeroed and `marginBottom -12/-6` cancels part of it → nets ~28px above / 16px below on desktop, 22/10 on mobile. Font unchanged (11px). Applied byte-identical to prod (`src/App.jsx`, committing this session) and the non-git demo (`Westcoast-demo/src/App.jsx`); demo fallbacks in `.fallbacks/App.jsx.fallback-*`. **Demo re-synced to prod:** demo's `src/data.jsx` had drifted (missing Chief→Scallion & Smoke→Miso Ginger Soy from prod commits a238a56/47b5fa2) → copied prod's data.jsx over it, restoring calc parity; demo now differs from prod ONLY on the 3 intentional demo-layer files (App.jsx demo-mode AI mock + label, Login.jsx auto-login, supabase.js offline localStorage mock). **Demo `dist` rebuilt twice** (footer, then Option B) so the shared build reflects everything — old builds snapshotted `.fallbacks/dist.fallback-*`, new bundle verified to contain footer + data fixes + no source maps. User confirmed all good. Prod data.jsx was already committed (47b5fa2), so this commit is footer-only.
- **Ownership/copyright footer COMMITTED 24f5d6d (2026-07-21, this session, user-verified):** Added a muted, centred footer at the bottom of the main app container — `© 2026 ELITE LVL SERVICES UK LTD. All rights reserved.` (company name is a hover link to https://www.elitelvlservices.com/, new tab, noopener noreferrer) + `Unauthorised use or reproduction prohibited.` (British spelling, UK Ltd registered name spelled out per user). Applied to all 3 envs: dev/prod source (src/App.jsx, committed to main) and the non-git demo (Westcoast-demo/src/App.jsx) — footer byte-identical across both (parity confirmed via diff). Demo fallback snapshot saved: Westcoast-demo/.fallbacks/App.jsx.fallback-20260721-114757. Build passed clean; user confirmed it renders correctly (browser-pane verify was blocked by another session's server holding the folder, so user did the visual check). Revert point = a238a56. Next: promote dev→prod deploy (Vercel redeploys from main) if not automatic.
- **Menu seed corrections vs. spreadsheet (2026-07-21, this session — local edit to `src/data.jsx`, NOT committed):** Checked all 8 classic bowls' ingredients against the menu spreadsheet image. Three gaps found; user's calls applied: (1) **Chief** was missing **Scallion** → added at 0.01 to all three sizes; (2) **Smoke** spreadsheet sauce is **Miso Ginger Soy** but seed only had Yuzu Sriracha → **added** Miso Ginger Soy 0.03 to all sizes (Yuzu left in; user will correct in-app on first login); (3) extra plain **Shoyu** in Pacific/Cascade/Coast/Smoke/Chief (not on spreadsheet) → **left as-is** per user (correct in-app if needed). Ingredient index `usedIn` updated too: Scallion +Chief, Miso Ginger Soy +Smoke. Reminder to user: these only reach the app after **Resync from seed** (or fresh `seedIfEmpty`) in each environment; demo needs its own resync if it's a separate Supabase project, and resync overwrites in-app edits. Aloha/Dynamite/Teriyaki matched clean.
- **Items 19–27 SHIPPED & user-verified (2026-07-20, this session, closed clean):** Full UI batch committed/pushed (091bbff + 11498b6) and confirmed working on localhost by user. (19) Dashboard: What to push left of Location comparison, borderless tinted stat/action cards, blue/amber accent borders on comparison & Buy it today. (20) Insights: Ask + Focus paired side by side, tinted borderless, List view default. (21) Sales narrative resting blueL tint after bar+shimmer, at 75% alpha so the down-month fish shows through (fish unchanged). (22/27) Annual target card → minimal fading half-donut gauge: pace-tint colour with seamless Excel-style fade, flat green needle (#5CB85F, 0.85 opacity, drawn behind the centre %), yellow even-pace dot (#F5B93E, arc-width, white outer ring), % only in centre (×N.N past 150%), legend row + 4 native SVG title tooltips, readout line under header, dial scales with card height capped 210px. (23) Month explorer LEFT + target card RIGHT, split aligned to charts row. (24) wpLift hover lift on all cards. (25) Plain-English location comparison (cents-per-dollar wording; >100% shows data-sanity flag), Enter monthly sales left of Edit month. (26) Data health → 3 floating concentric rings (red month entered / green costs fresh / blue samples) + legend with per-ring status. Year-target grand celebration: celebrated_<year> flag REMOVED — replays on every below→above crossing in-session, never on plain page load (old settings flag now ignored, no cleanup needed). Also committed with the batch: other session's owner-email swap files + add-missing-menu-items.sql. Process note: mid-session the user re-affirmed HARD that nothing gets edited without a visualised mockup + explicit approval first — memory updated. Only supabase/.temp/ left untracked (junk, gitignore candidate).
- **Item 31 BUILT 2026-07-20 (local only, NOT committed) — AWAITING USER VISUAL TEST:** Ingredient price chart (`PriceChart`, src/data.jsx) was rendering ~690px tall on wide desktops and showing a near-flat line. One root cause, three fixes: (1) SVG had `width="100%"` + `maxWidth:"100%"` on a 460x160 viewBox, so it scaled ~4x with container width — now capped at maxWidth 560px; (2) y-domain padded off absolute price (min*0.92/max*1.08) rather than spread, giving a 90c Ahi range a $15-$19 axis — now half-span = max(range/2, mid*3%, 5c) +30% pad, so Ahi fills 68% of the height while a 3c-noise item stays at 4%; (3) gridline labels toFixed(0) -> toFixed(2) (would collide on one integer once the domain tightened). User CONFIRMED the chart fix live (axis reads $16.29/$16.95/$17.61, climb now legible). Items 31a+31b USER-VERIFIED live on demo 2026-07-20 (light mode: "↗ 5.5%" no sign, chevron ▸ now visible): direction arrows on the collapsed row (cell width 52->62), sign dropped so it reads "↗ 5.5%" / "↘ 2.1%" (arrow+colour carry direction), "flat" in muted grey for |change|<1% (user picked the cutoff), and the expand chevron bumped from an all-but-invisible 11px/muted to 15px/T.slate — it was never missing, just unreadable on dark. Collapsed row otherwise untouched — user rejected sparklines there ("swimming sperms"). ALL of item 31 + 31a applied to BOTH /Users/wenceslyne/Westcoast and /Users/wenceslyne/Westcoast-demo (PriceChart byte-identical across the two); both `npm run build` pass. NOTE: the demo folder is NOT a git repo — no version control, edits there are unrecoverable. Demo could not be started this session (another chat holds port 5199, which user says the demo requires).
- **Item 30 COMMITTED & pushed 63c6d46 (2026-07-20, this session):** Sign-out button now needs a confirm click (pill: "Sign out? Yes / ✕", auto-reverts after 4s; mobile shows "Sign out ✕") to prevent accidental log-outs — motivated by Supabase's auth-email rate limit making re-login costly. User verified working live (desktop + mobile). Diagnosis this session: no sign-in limit exists in our code; single-session-per-user confirmed OFF; the limit users hit is Supabase's built-in email sender (~2 auth emails/hr + 60s cooldown). REMAINING (user, dashboard): custom SMTP (Auth → Emails → SMTP, e.g. Resend) then raise email rate limit (Auth → Rate Limits) before onboarding the other 2 owners. (Note: an earlier session binned Resend/SMTP as unnecessary — rate-limit pain during multi-device testing reopened it.)
- **Docs session (2026-07-20, closed):** Created MANAGEMENT-PLAYBOOK.md (internal manager/dev/PM playbook — architecture, process, costs, security, current state, roadmap, checklists), Westcoast-Poke-Management-Playbook.docx (downloadable Word version), and Food-Cost-Intelligence-Sales-Deck.pptx (9-slide external client pitch deck, anonymized). All three uncommitted, in repo root — user accepted; decide later whether to commit (deck probably shouldn't live in the app repo). Deck/doc not pixel-previewed (no LibreOffice on this Mac) — user to eyeball in PowerPoint/Word before external use. No code touched.
- **Items 28+29 COMMITTED & pushed (2026-07-20, this session):** (28) Help button in header (pill left of Sign out, ? icon) opening mailto:wenceslyne@elitelvlservices.com, subject "Westcoast Poke Support Request", body pre-filled with the logged-in user's email; V2 (in-app AI support chat + summarized transcripts + owner inbox) parked in ROADMAP under Committed parks. (29) Reactive hover on header/regenerate buttons: wpHovG (outline → lift + blueL/blue) and wpHovS (solid → lift + blueDark) classes, theme-aware via --wpBlue vars on the app root; applied to Scan receipt, Help, Sign out, dark toggle, unselected location pills, Refresh live prices, ✦ Regenerate. User verified visually. Follow-up 28a committed bf55a0a: Help button swapped to a real <a href="mailto:"> so right-click offers native "Copy email address" (desktop Chrome without a mail handler silently drops mailto left-clicks — user's phone works; desktop needs chrome://settings/handlers or a default mail app). User confirmed working.
- **Empty-state batch COMMITTED 379fee9 (2026-07-20, this session):** User wiped all test data (deleted line items themselves; then ran `delete from sales;` — plus optionally receipts/ingredient_prices/alerts — in SQL Editor per our advice; cleared AI insights via `localStorage.removeItem("wp_insights")`). Fixes shipped: (1) HealthRing rings always draw a 12%-opacity full-circle track so score 0 shows ghost rings, not blank; (2) dashboard keeps layout with no months — dashed $0.00 "This month" cards, Location-comparison placeholder, ghost Renegotiate/Review-pricing action cards (user chose to keep all 3 action slots visible); (3) narrative guard: a month totaling $0 says "logged but has no sales yet" and can never be "strongest month". Volatility alert boxes deliberately left with no placeholder (they're alerts). Pushed to GitHub (091bbff..6ac86c7) 2026-07-20 — Vercel redeploying. User verified placeholders visually in their own preview; narrative guard compiles but untestable until data exists.
- **Owner email swap COMPLETE (2026-07-20):** old sbgomez604@gmail.com / r_gomez_02@yahoo.com replaced with sheila@westcoastpoke.com / richard@westcoastpoke.com in all 3 allowlists: src/supabase.js OWNER_EMAILS (committed/pushed by user → Vercel), security-sessions.sql (user re-ran in SQL Editor), supabase/functions/anthropic-proxy/index.ts (deployed via `npx supabase functions deploy anthropic-proxy` — CLI now logged in + linked on this Mac). Old emails never had auth users, so no sessions to purge. REMAINING: user deliberately holding off on inviting the 2 new owners (Auth → Users → Add user → Send invitation) — needed before they can log in, since signups are disabled.
- **Items 13–18 + polish ALL SHIPPED & user-verified (2026-07-20, session closed clean):** Everything on CHANGELIST through item 18 is live on Vercel and confirmed looking good. Final commits this session: 5930088 (comparison card earns its height: 22px figures, 10px bars, vs-last-month line, insight pinned; row-2 full heights; Security tab spacing/"Access & security" space), 6659400 (down-month fish = chubby weave around the Sales narrative, full-screen leap removed), 3a35ab7 (all dashboard/Sales side-by-side grids → minmax(0,1fr) so rows split at identical points). Item 15 celebrations live: confetti (monthly save), chubby fish + "Keep swimming" (down months), sparkle (scans), pulse (samples), grand rain + trophy (yearly target, once via celebrated_<year> flag). Health ring + bell + streaks live. OPEN for next session: parked items only — item 8 (in-app search + BYO keys), POS add-ons integration (Sales row 5 stub), delay tuning, optional SerpAPI-via-Vercel-function for cheaper samples, optional CHANGELIST cleanup (statuses stale in places). No uncommitted code.
- **V1 batch DEPLOYED + key rotation complete (2026-07-20):** Batch committed/pushed (d503e3e) incl. the other session's anthropic-proxy Edge Function. User deployed the function via dashboard (JWT verify off), set ANTHROPIC_API_KEY secret (fresh key, never-expire), deleted VITE_ANTHROPIC_KEY from all Vercel envs, redeployed, disabled the old exposed key. Live bundle verified: no sk-ant, no direct api.anthropic.com, proxy referenced. AI features confirmed working live (user saw What-to-push data). Follow-up dashboard tweaks pushed: b7c176d (comparison+push side by side), 50b31fd (best price + buy-it-today side by side), c3ee786 (push top-3 only, all row cards equal 1fr 1fr). Login.jsx: magic link now redirects to requesting origin; localhost:5173/** added to Supabase redirect allow-list, Localhost magic-link login retested 2026-07-20: WORKS (link honors the localhost redirect). Resend/custom-SMTP idea BINNED per user decision — not required; email stays link-only (the login screen's code input is dormant, harmless).
- **Big V1 batch built (2026-07-20, NOT committed):** CHANGELIST items 6, 6b, 7, 9, 10, 11, 12 all built in src/App.jsx (+ new PLAYBOOK.md). (6) Opt-in market tracking — all tickboxes default unticked; per-ingredient search terms (settings key `search_terms`); mid-month/month-end market sample banner running the existing Anthropic web-search per location postcode (V5Y 3Z5 / V7A 5J3), samples logged in settings key `market_samples` and stored via saveMarketChecks (note: SerpAPI swapped for Anthropic web search — no key/backend exists). (6b) Free "Buy it today" Google-search card on the dashboard. (7) Volatility insight cards (≥12% swing over recent receipts) with one-click Track. (9+11) All paid buttons (insights, scan, live prices, discovery, market sample) go through confirm-with-last-updated → 3s/10s cancellable countdown ($0 if stopped); theme toggle moved after Sign out; insights-refresh button restyled blue. (10) Desktop density pass: tighter card padding, slimmer hero/target/YTD (splits on hover), Menu 3-col grid, compact Insights banner, 2-line "more" clamps on advisory copy. (12) Sales rows reordered: summary → Menu cost analysis → COGS+Top3 side by side → Units full width → Top 5 add-ons V2 stub. Vite build passes; app boots to login clean; NOT visually tested behind login.
- **Template simplified to $ totals for sides/drinks (2026-07-20):** Supersedes the earlier per-item add-on rows (removed — user's Lightspeed POS sells add-ons as generic "Topping Add On"/"Add Protein" buttons, so per-ingredient counts were unfillable). Template = bowls with S/M/L counts + two "Totals" rows (Sides — total $ sold, Drinks — total $ sold, One-size column). Parser stores them in `mix.totals={sidesRevenue,drinksRevenue}` per location (money parser accepts $/commas); old per-unit months still work (`legacyPerItemUnits`). `buildDataSummary` extras now reports sides $, drinks $, and `impliedAddOnsAndFees` = other revenue − sides − drinks (all labeled indicative); insights prompt asks for one revenue-mix insight. Dashed "Upload POS report · V2" placeholder button added next to Upload counts (shows a coming-soon toast). `.claude/launch.json` now has `autoPort: true`. All in src/App.jsx; compiles, app boots clean. User confirmed 2026-07-20: template download + upload both work. NOT committed yet.
- **Supabase menu_items schema fixed (2026-07-20):** Table was missing `sizes` and `category` columns — this is why the template only showed 7 bowls and why the Resync button silently failed (`resyncMenu` never checks the supabase-js error; upsert with onConflict also needs a unique constraint on `name`). `add-missing-menu-items.sql` (repo root, not committed) adds both columns + inserts the 27 seed items guarded by `where not exists`. User has NOT yet confirmed running the final version. Follow-ups approved-in-principle but not built: surface errors in resyncMenu/seedIfEmpty; unique constraint on menu_items.name.
- **2026-07-20:** Dark-mode fix — "GENERATED ..." label in the AI Insights headline banner was hardcoded white and invisible in dark mode; now uses `T.bg` at 55% opacity (src/App.jsx, Insights component).
- **2026-07-20:** AI Insights now persist across reloads via localStorage key `wp_insights` (insights + generatedAt). Only overwritten when the user clicks Generate/Regenerate. Banner shows the stored generation date. Not yet pushed to GitHub/Vercel — local only.
- **CL-3 (fixed 2026-07-19):** Ingredient tickboxes (plus custom-ingredients-without-price and size mix) weren't persisting because the `settings` table was missing from Supabase. Fixed by creating the table directly in Supabase SQL Editor (`key text primary key`, `value jsonb`, RLS with authenticated all-access policy). No app code changed — `saveSetting` in `src/db.js` works as-is. User confirmed fixed.

## Notes
- Repo cloned to `/Users/wenceslyne/Westcoast`, connected to `origin` (github.com/wenceslyne-git/westcoast-poke-cost). Push access not yet tested.
- Standing rule: Supabase schema changes run as SQL in the dashboard, not via GitHub uploads.
- Standing rule (see CLAUDE.md): discuss and get explicit approval before making any code changes or commits.

## Done (cont.)
- **CHANGELIST.md items built (2026-07-20):** (1) Dashboard YTD strip — 4 compact tiles under the monthly KPI row with per-location splits; food cost %/profit computed only over months with bowl counts, footnoted. (2) Quick-win treatment on "Best price today" / "What to push" cards — badge, bigger title, payoff subtitle, accent border. (3) Sales tab defaults to last month, falling back to most recent month with data. All in src/App.jsx. Compiles (esbuild check); NOT visually tested (app is behind owner login) and NOT committed — awaiting user test + commit approval.

- **UI state persistence (2026-07-20):** Active tab, location filter, and dark mode now persist across reloads via localStorage (`wp_tab`, `wp_loc`, `wp_dark`) in src/App.jsx. Compiles; not visually tested (behind login), not committed.

- **Committed & pushed (2026-07-20):** All 4 changes pushed to main (e8e0692) — Vercel redeploys from it. GitHub auth now works from this Mac: gh CLI at ~/.local/bin/gh, logged in as wenceslyne-git, git credential helper configured. .gitignore added (node_modules was previously unignored).

- **Dashboard simplification + annual target (2026-07-20):** Monthly KPI row removed; YTD cards promoted to hero size; new year-target card (inline entry, saved to settings key `target_<year>`) with progress bar + even-pace marker + ahead/behind readout. src/App.jsx. Also: YTD cards now respect the location selector (splits only on All). Committed & pushed 2026-07-20 (second batch); not yet visually verified by user.

- **Anthropic key moved server-side (2026-07-20, NOT committed/deployed):** New Supabase Edge Function `supabase/functions/anthropic-proxy/index.ts` holds the API key as a secret and only serves owner emails (403 otherwise — also enforces no-AI-for-demo server-side later). src/App.jsx: `API_HEADERS` replaced with `claudeFetch(body)` helper; all 7 direct api.anthropic.com call sites swapped. Build passes; served bundle verified free of key references. **AI features are broken until the function is deployed + secret set** (steps given to user: `supabase functions deploy anthropic-proxy`, set ANTHROPIC_API_KEY secret, remove VITE_ANTHROPIC_KEY from Vercel env, rotate the old Anthropic key). Standing rule saved to memory: never ship secrets client-side.
- **Demo access design agreed (2026-07-20, NOT built):** Code-based demo login (Supabase anonymous auth + `claim_demo_session` RPC), max 5 concurrent sessions, 10-min expiry, read-only UI with toasts, AI calls stubbed, RLS blocks writes. User confirmed real data OK for demo. Build after the key fix is deployed.

## Next — to-do (as of 2026-07-20, template batch pushed as d4f44b5)
- [x] Security batch DONE & verified (2026-07-20): Edge Function deployed (unauth probe returns our 403), secret set, VITE_ANTHROPIC_KEY removed, old key rotated, claudeFetch code + SECURITY.md + security-check.sh + .env* gitignore committed/pushed (6ac49dd), user confirmed AI works on live site.
- [ ] **V2 roadmap** (moved 2026-07-20, user decision): demo access layer (code login, 5 sessions, 10-min expiry, read-only).
- [ ] **Onboarding security batch (2026-07-20, in progress):** (a) User to do in Supabase dashboard: Auth → disable "Allow new user signups"; set OTP/magic-link expiry 300s; confirm time-boxed sessions/inactivity timeout off. (b) Built: resyncMenu/seedIfEmpty now throw supabase-js errors (src/db.js) + resync toast shows the message; add-missing-menu-items.sql now also creates unique index menu_items_name_key (user still to run in dashboard). (c) Emails confirmed; client allowlist already existed (src/supabase.js OWNER_EMAILS + isOwner gates in Login.jsx and App.jsx) — nothing new needed. Security tab BUILT (mockup approved): nav item + SecurityTab in src/App.jsx (allowlist card + active sessions via rpc list_active_sessions, force-end via rpc end_session; own session disabled; owners keep sessions indefinitely per user decision — no timed expiry, force-end takes effect within ~1h JWT expiry). Backend = security-sessions.sql (2 security-definer RPCs on auth.sessions, guarded by the 3-email check) — run in SQL Editor 2026-07-20 (needed explicit ::type casts on every column, fixed in the file) and user confirmed the tab works live. Signups disabled + menu SQL run + Security tab verified — onboarding security batch COMPLETE (incognito blocked-email spot-check passed too). Clear to onboard the other 2 owners. Session limits/timeouts were for demo users only → V2 decision. (d) security-check.sh run: 4 pass / 2 warn (key strings in git history — already rotated; npm audit: esbuild dev-server moderate + xlsx high w/ no fix, both low real risk — xlsx only parses owner's own uploads).
- [x] Security policy written → SECURITY.md (2026-07-20, not committed): secrets server-side only, RLS everywhere, server-enforced authz, exposure-response steps, pre-deploy checklist. Copy into future projects as the template.
- [ ] **FIRST (paused mid-verify 2026-07-20):** YTD cards overflowing on "All Locations" — fixed in src/App.jsx line 641 (`repeat(4,1fr)` → `repeat(4,minmax(0,1fr))`, same for the 2-col mobile case) so tracks can shrink and split lines ellipsize. Uncommitted. User's preview still showed the overflow, most likely a stale server/cached bundle (another session's server on 5173; this session's on 5175). Next: hard-refresh (Cmd+Shift+R) on the dev server, confirm the 4 cards fit and split text truncates with "…", then commit.
- [ ] User verify on deployed app: fresh template shows the two Totals rows ($ sides / $ drinks) and no add-on rows.
- [ ] User verify: upload a template with a deliberate bowl count in the One-size column → import message shows the per-row warning naming the bowl.
- [ ] User verify: save a month with $ totals, regenerate AI Insights → a revenue-mix insight (bowls vs sides vs drinks vs implied add-ons/fees) appears, labeled indicative.
- [ ] User confirm: the final add-missing-menu-items.sql (with the two `alter table` lines) was run in Supabase → template lists all 10 bowls. If not run yet, run it, hard-refresh, re-download.
- [ ] Optional housekeeping: set git identity (`git config --global user.email wenceslyne@elitelvlservices.com` + user.name) — d4f44b5 went in as wenceslyne@mac.lan.
- [ ] Approved-in-principle, not built: surface supabase-js errors in resyncMenu/seedIfEmpty; add unique constraint on menu_items.name so the Resync button works.
- [ ] V2: parse an uploaded POS monthly report to fill counts automatically (placeholder button already in UI); shape add-on rows to whatever lines her PoS report actually shows.
- ~~Known issue: bowl count in One-size silently ignored~~ Fixed 2026-07-20: parser now warns per row whenever a bowl row has a nonzero One-size value; zero-fills stay silent. Value is still ignored (not imported). Untested by user (covered by checklist above).
- 2026-07-20 dashboard batches (both) deployed and user-verified. Nothing in flight.
- Reminder for user: July 2026 still has no bowl counts entered, so food cost/profit read $0 until the July mix is uploaded on the Sales tab. (YTD strip, quick-win cards, Sales default month, UI state persistence).
- Untracked, deliberately not committed: add-missing-menu-items.sql (Supabase SQL runs in dashboard per standing rule), package-lock.json (recommend committing to pin deps for Vercel — needs user OK).
- Open question for user: July 2026 sales has a $210 total but no bowl counts entered, so food cost/gross profit show $0 — they need to enter July's bowl mix on the Sales tab.
